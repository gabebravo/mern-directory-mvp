import React from 'react'

const ListingFloor = ({ floor }) => (
  <div className="row">
    <div className="twelve columns">
      <h3>{`${floor} Floor`}</h3>
    </div>
  </div>
);

export default ListingFloor
